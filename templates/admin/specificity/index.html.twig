{% extends "base.html.twig" %}

{% block title %}
    Spécificités
{% endblock %}

{% block body %}
    <div class="container-fluid">

       {% include "admin/partials/miniHeader.html.twig"  with { Title  : 'Spécificités', Link : path('AdminDashboard.index') } %}

        <h1 class="h1 my-3">Gestion des spécificités</h1>

        <a href="{{ path('AdminSpecificity.add') }}" class="btn btn-success my-5">
			<i class="fas fa-plus-circle"></i>
		</a>

        <table class="table table-hover">
			<thead>
				<tr>
					<th>Id</th>
					<th>Libellé</th>
					<th class="text-center">Action</th>
				</tr>
			</thead>
			<tbody>
				{% for specificity in paginator.data %}

					<tr>
						<td>{{ specificity.id }}</td>
						<td>{{ specificity.wording }}</td>
                    	<td class="text-center">
                            <a title="Modifier la spécificité" href="{{ path('AdminSpecificity.edit', {'id' : specificity.id}) }}" class="btn btn-primary mr-2">
								<i class="fas fa-edit"></i>
							</a>
                            <a href="{{ path('AdminSpecificity.delete', {'id' : specificity.id}) }}" class="btn btn-danger ml-2" title="Supprimer la spécificité">
									<i class="fas fa-trash"></i>
							</a>
                        </td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		{{ paginator.display }}
    </div>
    
{% endblock %}